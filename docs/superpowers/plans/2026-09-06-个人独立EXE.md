# 个人独立EXE实施计划

目标：桌面单EXE无需安装Python或携带配置，默认使用指定在线辅助。
架构：复用PyInstaller、原配置校验和GUI；构建时注入个人资源。
设计：docs/superpowers/specs/2026-09-06-个人独立EXE-design.md。
执行：串行，master，不启动代理或新分支；已有自主执行授权。

- [x] 阶段1需求澄清：单文件、内置、桌面、本人使用和清理边界确认。
- [x] 阶段2方案设计：包内资源优先，密钥不进仓库。
- [x] 阶段3计划：先测试配置读取，再打包、独立运行验收、回收临时材料。
- [x] 阶段4实现：tests/test_默认大模型配置.py补充真实GUI测试，先失败；gui.py在冻结运行时优先Path(__file__).with_name('大模型默认配置.json')；原打包cmd加入--add-data并指定专用工作目录。只在临时目录生成构建产物。
- [x] 阶段5验收：定向配置/GUI/客户端测试及全套pytest；正式EXE用空白LOCALAPPDATA和仅Windows的PATH启动，验证GUI、内置配置、API连接和构造报告，检查算法不变与源码不含密钥。
- [x] 阶段6交付：更新README、验收文档；桌面EXE校验哈希；源码提交推送master，成品不上传；列清单将本轮临时目录移入回收站，核验已清理。

测试使用本机Python314，-B -X utf8 -m pytest -q -p no:cacheprovider，--basetemp位于本轮专属目录。打包执行原cmd，BANK_BUILD_ROOT、BANK_DIST_DIR、TEMP、TMP及PYINSTALLER_CONFIG_DIR指向本轮工作目录下独立路径。源文件先备份并校验再改；桌面同名成品若存在同样备份。禁止永久删除。

用户最新指令收窄验收：停止反复打开软件。已完成构建、配置核验、GUI启动及全量测试；成品GUI在线按钮和整流程报告测试未完成，不再继续，残余限制在验收中披露。阶段6按桌面文件、回收与master推送结果完成。

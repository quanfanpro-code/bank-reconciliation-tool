# 单账户业务匹配实施计划

> 执行方式：按requirement-workflow内置executing-plans逐项执行，独立文件任务并行，任务完成后交叉审查。用户已授权自主决策，不再请求阶段确认。

目标：单账户双向拆并、多对多和不等额工资完整核对，同时保留真实歧义。
架构：沿用现有候选流程，在业务分组、资格、选择和分流之间共享判断；原输入和报告接口保留。
技术：本机Python 3.14、现有pandas/openpyxl/pytest，无新增依赖。
设计：C:\Users\27651\Documents\Code\银行流水核对工具\docs\superpowers\specs\2026-09-05-单账户业务匹配-design.md

## 全局约束

- 只修改指定项目，原银行及账簿资料只读。
- 主程序入口保持main.py；单账户两表；不新增用户必答配置。
- 中文注释、消息和文档；中文文本UTF-8，适用时BOM。
- 修改已有文件前备份核验，现有文件基线备份目录为C:\Users\27651\BackUp\银行流水核对工具_20260905_162424。
- 一个文件由一个执行者串行修改。并行任务只写各自文件。
- 使用C:\Users\27651\AppData\Local\Programs\Python\Python314\python.exe -X utf8。
- 每次测试指定新的work临时目录，避免清理旧文件；不删除文件，不提交、不推送。

## 任务1：核心业务匹配

归属：核心执行者。文件：matcher.py、matching_policy.py；按需新增业务分组.py；新增tests\test_单账户业务匹配.py。
输入：现有标准DataFrame，含date、amount_decimal、summary、aux_text_fields、voucher_no和原行号。
输出：既有MatchCandidate；evidence.business_basis为中文依据，evidence.alternative_candidate_ids为仍合理的竞争编号列表。其他新证据仅内部使用。

- [x] 失败测试：将7个构造业务样本变成真实Matcher.run断言，期望完整组成与状态。至少如下：
~~~python
matcher.run()
confirmed = [c for c in matcher.selected_candidates
             if c.processing_status.value in {"自动确认", "整组勾稽一致"}]
assert any(c.bank_idxs == (0,) and c.journal_idxs == (0, 2)
           for c in confirmed)  # 1,000对应400和600，排除200
~~~
- [x] 运行新增测试，确认业务失败，不是导入或fixture错误；记录红灯。
- [x] 最小实现：从原始业务依据形成完整组；统一合理竞争、选择和分流。以已有数据结构承载，不做规则平台。禁止零差无条件优先。
- [x] 加入反例：强P01差10元对比弱同额、不同工资批次、同额真多解、跨年度同号、不明编号不强推业务身份。
- [x] 定向验证：新增业务测试、test_candidate_pipeline.py、test_automation_first.py、test_reliability_upgrades.py。
- [x] 核心自审并交叉审查，记录实际命令、红绿结果及限制。

## 任务2：输入建议和报告含义

归属：主执行者。文件：gui.py、reporter.py；必要时data_loader.py；新增tests\test_业务证据与报告.py。
依赖：任务1的两个evidence键；其他接口不变。

- [x] 失败测试：自动建议选入“业务编号”“批次号”而非账号；原编号前导零保留；零差疑点不计入组级已勾稽率。
~~~python
selected = auto_select_auxiliary_columns(["摘要", "业务编号", "批次号", "银行账号", "凭证号"])
assert "业务编号" in selected and "批次号" in selected
assert "银行账号" not in selected and "凭证号" not in selected
~~~
- [x] 确认红灯后局部实现输入建议、业务依据和竞争原行说明。
- [x] 保留全部组成，统计银行与账侧笔数/金额，组级口径只计算已确认关系；保持旧候选缺少新字段时兼容。
- [x] 定向验证GUI辅助字段测试、报告测试、真实Excel生成回读。
- [x] 交叉审查输入、输出与核心对同一关系的解释一致。

## 任务3：集成、性能和桌面入口

归属：主执行者；审查者只读。
- [x] 再次运行7个代表样本，确认已解决初始业务缺口并保留真多解。
- [x] 构造有干扰记录的几千、上万行数据及大工资批次，比较升级前后；输出组成正确率、运行时间、内存和搜索覆盖事实，不能把构造测试当真实项目准确率。
- [x] 如发现性能/取消故障，先建立失败用例再改共同根因；不盲目放大上限。
- [x] 全量验证：
~~~powershell
& 'C:\Users\27651\AppData\Local\Programs\Python\Python314\python.exe' -X utf8 -m pytest -q -p no:cacheprovider --basetemp <本次新建work目录>
~~~
- [x] 通过main.py真实Windows入口，选入代表Excel、运行并回读报告；原资料哈希保持一致。
- [x] 执行最终需求对照、代码和简洁性审查，修复后重新验证。

## 任务4：文档和交付

归属：主执行者。
- [x] CONTEXT.md更新已澄清术语，不写实现细节；README说明实际行为及使用边界。
- [x] docs\reviews\2026-09-05-单账户业务匹配-review.md记录发现、修复、测试及残余限制。
- [x] 在本次outputs下交付简洁说明及代表核对结果；原入口仍可使用。
- [x] 保留本地变更，不提交、不推送、不删除分支或原文件。
- [x] 六阶段全部完成后报告真实验证结果。

## 计划一致性核查

| 任务关系 | 共享接口/范围 | 结论 |
| --- | --- | --- |
| 任务1/任务2 | MatchCandidate.evidence两个展示键 | 核心写、报告读；缺失用空值兼容，文件无并发覆盖 |
| 任务1/任务3 | Matcher.run真实入口 | 先核心红绿，再集成实测；不复用旧成绩 |
| 任务2/任务3 | 原gui→application→reporter入口 | 保留原调用链，验证真实Excel |
| 任务1自身 | 确定关系和多解反例 | 零差、分数及组标记均不能绕过证据 |
| 任务2自身 | 报告覆盖率与状态 | 零差疑点不算已核对 |
| 任务3自身 | 测试规模 | 自主构造并实测，不要求用户提供技术参数 |
| 任务4自身 | 发布动作 | 只本地交付，不把提交推送作为必要步骤 |

最终执行证据：268项全量通过，91项独立Excel检查通过；原桌面入口终验通过。详见docs\reviews\2026-09-05-单账户业务匹配-review.md。六阶段已完成，本地保留原路径变更。

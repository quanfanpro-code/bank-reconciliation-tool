# 报告阅读与人工复核展示实施计划

目标：只修改报告输出，使月度合表、逐笔并排、整组折叠与就地人工选择可以直接使用。
架构：复用既有计算表，展示投影与排版集中在报告展示.py；复核报表.py只接入展示函数，底稿筛选.py只适配显示表。
技术：现有Python、pandas、openpyxl、本机Excel，无新增依赖。
设计：docs/superpowers/specs/2026-09-06-报告阅读与人工复核展示-design.md。
授权：用户已批准继续执行，后端变更必须另行批准；原有README更新、提交和推送授权继续有效。

## 六阶段

- [x] 需求澄清：用户已确认内容和仅输出边界。
- [x] 方案设计：采用现有数据的展示投影，用户已批准逐笔/整组分表和展开结构。
- [x] 实施计划：按以下顺序推进，日常实施自主完成。
- [x] TDD编码执行。
- [x] 验收审查。
- [x] 交付完成。

## 任务1：分表、组成与唯一填写入口

文件：新建报告展示.py、tests/test_报告阅读布局.py；修改复核报表.py两个展示接入点。保留原始核对明细和全部原公式。

- [x] RED：生成2对3报告，断言逐笔表不含该关系，整组存在主行+5条组成，银行组成另一侧为空，每个原始记录只出现一次；断言只有业务入口可见。当前版本应缺少整组核对表而失败。
- [x] GREEN：build_display_tables(tables)以事项和原始记录组织输出；format_display_sheets(book)引用既有结果并设置折叠、列宽、隐藏及链接。
- [x] VERIFY：定向测试，再运行tests/test_复核工作簿.py和tests/test_人工结果计算.py。后者必须在真实Excel下通过。

## 任务2：月度展示与筛选兼容

文件：报告展示.py、底稿筛选.py、tests/test_报告阅读布局.py。

- [x] RED：验证可见月度列同时包含双方收支和余额，空余额显示缺失；筛选整组仍保留全部组成、原公式和人工结果。
- [x] GREEN：仅重排现有月度列、添加输出年度发生额合计；筛选按新表名保持整项行可见，技术表隐藏。
- [x] VERIFY：运行月度取值、筛选保存、人工计算相关测试；全部原计算结果与修改前一致。

## 任务3：审查和交付

- [x] 读取第五阶段规则，独立审查范围、数据完整性、重复加总和人工入口。
- [x] 运行完整pytest，实际入口生成样例；Excel重算与保存后回读，查看默认汇总和展开组成页面。
- [x] 检查后端文件无改动、输入文件哈希不变；更新README、CONTEXT展示术语及复核报告。
- [x] 读取第六阶段规则，检查最终差异和测试证据；提交推送当前分支，验证GitHub远端提交号，交付新样例及报告。

命令：在指定仓库通过 C:\Users\27651\AppData\Local\Programs\Python\Python314\python.exe -B -X utf8 -m pytest 执行测试，定向任务传入上述测试文件；以退出码0及明确通过数量为成功，不能复用此前588项通过证据作为本轮结论。

## 本轮验收记录

604 passed, 5 warnings in 279.75s (0:04:39)；本轮新增16项展示测试。4个场景前后原计算结果一致，20个核心文件不变。第五阶段独立复核通过，最终样例完整27条原始记录、12个月、Excel公式错误0个。详细验证与交付限制见 docs/reviews/2026-09-06-报告阅读与人工复核展示-review.md。提交推送使用已有工作分支，最终以本次提交的GitHub远端核验为准。

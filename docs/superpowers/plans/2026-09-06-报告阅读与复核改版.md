# 报告阅读与复核改版实施计划

执行方式：按内置 executing-plans 指引在当前工作分支串行执行。用户授权后续自主完成，不另开 agent。

目标：生成一份默认三张主表、业务清楚且完整可追溯的 Excel。

设计：docs/superpowers/specs/2026-09-06-报告阅读与复核改版-design.md。

技术：现有 Python、pandas、openpyxl；不增加依赖。项目根目录为 C:\Users\27651\Documents\Code\银行流水核对工具。

统一约束：不改匹配和风险抽样；来源只读；修改前备份校验；中文 UTF-8；当前工作分支提交推送，不合并其他分支。

## 任务 1：阅读数据与报告排版

新增报告阅读.py；修改 reporter.py 的表构造及最终排版出口；新增 tests/test_报告阅读改版.py。

接口：build_readable_tables(tables: dict) -> dict，接收旧证据及阅读来源记录，返回三主表与事项索引；apply_readable_presentation(workbook) 对已写入工作簿设置可见性、导航与格式。

- [x] 先写跨年小额场景及整组完整性、可见表、注入保护、低中风险留存测试。
- [x] 实际运行确认缺少三主表导致失败。
- [x] 最小实现来源记录、事项归并、主表构造、隐藏附表、折叠组成和内部链接。
- [x] 运行新增测试与既有报告、跨期、风险测试，再运行全量测试。

关键行为测试例：

```python
m = 核对([记录(3000, 日期="2025-12-31")], [记录(3000, 日期="2026-01-01")])
tables = Reporter(m).build_report_tables(m.config)
assert len(tables["待复核事项"]) == 1
assert "期间" in tables["待复核事项"].to_string(index=False)
```

## 任务 2：筛选导出衔接

修改底稿筛选.py，增加新格式分支，复用任务 1 构造与排版；旧格式保留原分支。测试加入同一文件。

- [x] 写筛选一组后双方记录完整带出、主表数量重算和源文件哈希不变的失败测试。
- [x] 以事项索引筛选，全组带出明细和附表，重建概览及链接。全局范围限制保留并说明出处。
- [x] 验证空筛选、中文条件说明、新格式及旧格式测试；运行全量测试。

关键行为测试例：

```python
export_filtered_workpaper(source, output, FilterCriteria(include_text=("甲公司",)))
overview = pd.read_excel(output, sheet_name="核对概览")
assert overview.loc[overview["项目"] == "银行记录笔数", "结果"].iloc[0] == 2
```

## 任务 3：验收、说明与交付

修改 README、CONTEXT 和需求进度；补充 docs/reviews/2026-09-06-报告阅读与复核改版-review.md。

- [x] 检查全部需求映射和残留技术用语，遇到缺陷先补失败测试再修复。
- [x] 实际桌面入口验收，回读源文件哈希、报告明细及筛选版，实际渲染样张。
- [x] 全量验证、编码检查、差异检查，记录新鲜证据；更新文档。
- [ ] 确认当前目录、分支与变更范围后，提交并推送，核对远端提交号。

统一验证命令（在项目根目录执行）：

```powershell
& 'C:\Users\27651\AppData\Local\Programs\Python\Python314\python.exe' -X utf8 -m pytest tests/test_报告阅读改版.py -q
& 'C:\Users\27651\AppData\Local\Programs\Python\Python314\python.exe' -X utf8 -m pytest -q
```

成功标准：新增行为测试通过、原有回归无未解释失败、仅三表默认可见、跨期等必要事项不遗漏、输出可读且来源完整；提交与远端一致。任何不能实施的视觉或真实数据验证如实记载，不能用静态测试冒充。

## 执行记录

新增测试先观察失败，再分批通过；最终完整验证为 552 项通过、5 条原有警告。真实桌面、Excel 渲染与公式、样例完整性及规模证据见同名验收报告。代码和文档准备完成，等待当前授权下的提交推送执行结果。

# 筛选交互易用性实施计划

按需求优先工作流及其串行执行指引完成。用户已明确授权修正上一轮列出的三项交互问题，沿用此前源码、README、提交推送授权，按当前主分支继续；不重复设置确认流程。

目标：无需记分隔符，选文件即获得选项，拖动排列列顺序。
设计：docs/superpowers/specs/2026-09-08-筛选交互易用性-design.md。
技术：当前Python314、Tk/ttk、pandas、openpyxl，不新增依赖。

- [x] 阶段1：三项问题及边界已依据用户指令确认。
- [x] 阶段2：复用既有筛选接口及桌面控件，方案和错误路径已记录。
- [x] 阶段3：制定下列实施与验收步骤。
- [x] 阶段4：TDD执行。
- [x] 阶段5：验收审查。
- [x] 阶段6：源码与文档提交推送。

## 任务1：条件编辑及自动加载

文件：筛选窗口.py、底稿筛选.py；新增tests/test_筛选交互易用性.py；调整旧测试的条件录入操作。
- [x] RED：实际报告传入窗口后无需预览按钮就出现选项；新增条件后build_criteria输出正确元组，修改删除及标点文字有效；加载失败、快速换报告和源文件更新行为可验证。
- [x] GREEN：拆出read_filter_report(source_path)返回事项及组成；summarize_filter(groups,components,criteria)给出原有预览结果；窗口增加选择文件入口、条件清单及单后台任务调度。
- [x] VERIFY：新交互测试和原筛选、列识别、人工结果测试通过。

## 任务2：拖动列

文件：筛选窗口.py、tests/test_筛选交互易用性.py。
- [x] RED：实际鼠标按下/移动/释放事件改变显示顺序，保存重开仍一致；键盘及按钮仍可操作。
- [x] GREEN：列表绑定拖动事件，复用同一重排操作；无选中行或越界不误改。
- [x] VERIFY：实际Tk事件、保存重开、列不丢失及原Excel选择计算通过。

## 任务3：审查与交付

- [x] 原生窗口截图检查条件、文字及按钮可见；此前真实报告自动载入和条件预览验证。
- [x] 本机Python运行完整pytest，记录退出码和数量；不打包。
- [x] README、CONTEXT和docs/reviews/2026-09-08-筛选交互易用性-review.md同步实绩与限制。
- [x] 检查范围及文件保护，提交推送master，核对远端提交。

执行命令：在C:\Users\27651\Documents\Code\银行流水核对工具，使用C:\Users\27651\AppData\Local\Programs\Python\Python314\python.exe -B -X utf8 -m pytest。先定向失败/通过，再运行全量，退出码0为通过依据。原调用链和接口由测试持续验证。

实际验收：684项全量回归通过；最终9项交互复测通过。真实报告自动加载48.414秒、后续预览0.365秒，370项结果与此前复算一致，源文件不变。

交付核验：源码和文档已提交并推送至master，提交ba201e3ffd2f9f33c63fcd845d70046673cd6c97，远端分支指向与本地一致。本轮未打包，未修改桌面EXE。

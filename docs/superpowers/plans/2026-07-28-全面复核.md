# 全面复核修复 Implementation Plan

> **For agentic workers:** Read `subagent-driven-development.md` when the user authorizes subagents; otherwise read `executing-plans.md` and implement task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 消灭 3 个致命 bug 与全部 P1 问题，顺手修 P2，readme 与实现对齐，全程 TDD。

**Architecture:** 保持现有 9 模块结构；定点最小修复 + 两处有边界收口
（期初余额提取 mapping 感知化、空余额语义 None 化）。

**Tech Stack:** Python 3, pandas, openpyxl, customtkinter, pytest。

## Global Constraints

- 既有 46 个测试必须保持通过（除明确说明的预期调整外）。
- 每个修复先写失败测试（RED）→ 最小实现（GREEN）→ 全量 `pytest tests/ -q`（VERIFY）。
- Windows 控制台 GBK：验证命令前缀 `PYTHONIOENCODING=utf-8`。
- 不执行任何 git 写操作；不引入新依赖；公开 API 仅 `extract_initial_balance` 新增可选参数。
- 新测试集中在 `tests/test_review_fixes.py`。
- 项目根：`C:/Users/27651/Documents/Code/银行流水核对工具`。

---

### Task 1: validate.py 上限与随机种子（P0-1 / P1-6）

**Files:**
- Modify: `validate.py:90-99`（upper_limits）、`validate.py:78`（random_seed 条目）
- Test: `tests/test_review_fixes.py`

**Interfaces:**
- Consumes: 现有 `validate_config_params(...)` 签名不变。
- Produces: 组合窗口上限 90、最大深度上限 100；`random_seed="-1"` 合法。

- [ ] **Step 1: 写失败测试**

```python
def test_GUI默认参数能通过校验():
    ok, msg = validate_config_params(
        "31", "31", "30", "3",
        random_seed="0", similarity_threshold="0.5", similarity_high="0.7",
        max_candidates="30", memory_limit="6.0", bank_skip="0", journal_skip="0",
    )
    assert ok, msg

def test_随机种子负一合法():
    ok, msg = validate_config_params(
        "31", "31", "30", "3",
        random_seed="-1", similarity_threshold="0.5", similarity_high="0.7",
        max_candidates="30", memory_limit="6.0", bank_skip="0", journal_skip="0",
    )
    assert ok, msg

def test_上限仍然生效():
    ok, _ = validate_config_params(
        "31", "91", "30", "3",
        random_seed="0", similarity_threshold="0.5", similarity_high="0.7",
        max_candidates="30", memory_limit="6.0", bank_skip="0", journal_skip="0",
    )
    assert not ok
```

- [ ] **Step 2: 运行确认失败**

Run: `PYTHONIOENCODING=utf-8 python -m pytest tests/test_review_fixes.py -q`
Expected: 前两个 FAIL（默认参数被拒 / -1 被拒）。

- [ ] **Step 3: 最小实现**

`validate.py`：
- int_params 中 `random_seed` 改为单独处理：允许 -1，其余 < -1 拒绝。
- upper_limits：`"组合匹配窗口": (dfs_window, 90)`、`"组合匹配最大深度": (dfs_depth, 100)`。

- [ ] **Step 4: 运行确认通过 + 全量回归**

Run: `PYTHONIOENCODING=utf-8 python -m pytest tests/ -q`
Expected: 全部 PASS（含既有 46 个）。

---

### Task 2: GUI 默认值对齐 readme（P1-7）

**Files:**
- Modify: `gui.py:293`（`allow_mixed_sign_var` 默认值）
- Test: 无需新测试（GUI 默认值，人工核对 diff）。

- [ ] **Step 1:** `ctk.BooleanVar(value=True)` → `ctk.BooleanVar(value=False)`（仅 allow_mixed_sign 一处）。
- [ ] **Step 2:** `python -m py_compile gui.py` 通过。

---

### Task 3: 批量聚合 category 崩溃（P0-2）

**Files:**
- Modify: `matcher.py:1284`（`group['summary'].fillna('')...`）
- Test: `tests/test_review_fixes.py`

**Interfaces:**
- Consumes: `Matcher.match_batch_aggregation()` 现有行为。
- Produces: category dtype summary 不崩溃；匹配语义不变。

- [ ] **Step 1: 写失败测试**

```python
def test_批量聚合_category摘要不崩溃且匹配():
    n = 12
    bank = pd.DataFrame({
        "date": [pd.Timestamp("2024-01-05")] * n,
        "summary": pd.Categorical(["代发工资"] * n),
        "amount_decimal": [500000] * n,  # 50.00 元 = 500000 厘
    })
    journal = pd.DataFrame({
        "date": [pd.Timestamp("2024-01-06")],
        "summary": ["代发工资总额"],
        "amount_decimal": [500000 * n],
    })
    matcher = Matcher(bank, journal, MatcherConfig())
    matcher.match_batch_aggregation()  # 修复前：TypeError
    assert matcher.bank["matched"].sum() == n
    assert matcher.journal["matched"].sum() == 1
```

（注：测试中直接构造含 amount_decimal 的 df，与既有 test_regressions.py 同模式；
date/summary/amount_decimal 之外列由 Matcher.__init__ 内部补齐。）

- [ ] **Step 2: 运行确认 TypeError**
- [ ] **Step 3: 最小实现**

`matcher.py` 第 1284 行改为：

```python
bank_summaries = group['summary'].map(lambda s: '' if pd.isna(s) else str(s)).str.lower().tolist()
```

- [ ] **Step 4: 单测通过 + 全量回归**

---

### Task 4: 空余额语义 None 化（P1-4，先行任务：Task 5/6 依赖它）

**Files:**
- Modify: `data_loader.py:759-764`（std_balance 解析）
- Test: `tests/test_review_fixes.py`

**Interfaces:**
- Produces: `standardize_data` 输出 `balance` 列——原始缺失/解析失败 → None，
  有值 → Decimal；下游按 None=缺失处理。

- [ ] **Step 1: 写失败测试**

```python
def test_空余额单元格为None而非零():
    loader = DataLoader()
    df = pd.DataFrame({
        "日期": ["2024-01-01", "2024-01-02"],
        "金额": [100, -50], "摘要": ["a", "b"], "余额": [1000.0, None],
    })
    mapping = {"date": "日期", "amount": "金额", "summary": "摘要",
               "voucher": None, "balance": "余额", "mode": "signed_amount"}
    res = loader.standardize_data(df, mapping, "bank", "auto")
    by_row = res.set_index("original_idx")["balance"]
    assert by_row.loc[1] == Decimal("1000.00")
    assert pd.isna(by_row.loc[2])
```

- [ ] **Step 2: 确认失败（现为 Decimal('0.00')）**
- [ ] **Step 3: 最小实现**

`data_loader.py` std_balance 段：先算 `balance_is_empty` 掩码（与 amount 相同的
  三条件），`clean_amount` 结果中 `balance_is_empty & (== Decimal('0.00'))` 置 None。

- [ ] **Step 4: 单测 + 全量回归**（重点关注 balance/reporter 相关既有测试）

---

### Task 5: 期初余额提取 mapping 感知 + 容错解析（P0-3 / P1-5）

**Files:**
- Modify: `balance.py:13-137`（`extract_initial_balance`）、
  `reporter.py:316-321`（传入 mapping）
- Test: `tests/test_review_fixes.py`

**Interfaces:**
- Produces: `BalanceRecalculator.extract_initial_balance(df, mapping=None) -> Decimal`
  —— mapping 可选，键 date/summary/balance/debit/credit/amount；
  任何单元格解析失败跳过该行，不再抛异常。

- [ ] **Step 1: 写失败测试**

```python
def test_期初提取_千分位余额不崩溃():
    df = pd.DataFrame({
        "交易日期": ["2024-01-01", "2024-01-02"],
        "业务说明": ["收款", "付款"],
        "账户余额": ["1,234.56", "1,134.56"],
    })
    mapping = {"date": "交易日期", "summary": "业务说明", "balance": "账户余额",
               "debit": None, "credit": None, "amount": None, "mode": "debit_credit"}
    # 修复前：InvalidOperation；且无 amount 列时应安全回退
    result = BalanceRecalculator.extract_initial_balance(df, mapping)
    assert isinstance(result, Decimal)

def test_期初推断_跳过空余额单元格():
    # 标准化数据：首日首笔 balance=None，应取首个非空余额行推算
    df = pd.DataFrame({
        "date": [pd.Timestamp("2024-01-01"), pd.Timestamp("2024-01-02")],
        "amount": [Decimal("100"), Decimal("-50")],
        "balance": [None, Decimal("950.00")],
        "original_idx": [1, 2],
    })
    result = BalanceRecalculator.extract_initial_balance(df)
    assert result == Decimal("0.00")  # 950 - (100-50累计)? 见实现注释
```

（第二个用例的预期值以实现约定为准：首个可用余额行之前的净额累加回补，
实现时写清注释并让测试断言该约定。）

- [ ] **Step 2: 确认失败（崩溃 / -100.00）**
- [ ] **Step 3: 实现**——mapping 列优先、clean_amount 取值、空行跳过、
  首个可用余额行之前的净额回补；Reporter 传 `self.bank_mapping/self.journal_mapping`。
- [ ] **Step 4: 单测 + 全量回归**

---

### Task 6: 期初警告的「无余额数据」分支（P1-4 下游 / reporter.py）

**Files:**
- Modify: `reporter.py:315-340`
- Test: `tests/test_review_fixes.py`

- [ ] **Step 1: 写失败测试**——任一侧无余额列数据时 `has_warning` 为 False，
  且汇总含「跳过期初核对」字样。
- [ ] **Step 2-4:** 实现（两侧 `balance` 列 `notna().any()` 判断）+ 回归。

---

### Task 7: 余额连续性异常 sheet（P1-8）

**Files:**
- Modify: `reporter.py:85-106`（列名/空值防御）、`reporter.py:619-630`（接入 sheets）
- Test: `tests/test_review_fixes.py`

- [ ] **Step 1: 写失败测试**——构造含余额跳变的标准化 df，断言
  `check_balance_continuity` 返回异常且 generate_report 产生的 sheet 列表含
  「余额连续性异常」。
- [ ] **Step 2-4:** 接入 + 回归（无余额数据时 sheet 不生成）。

---

### Task 8: 汇总表数字类型 + 明细确定性 + 涂色错位（P1-9 / P1-10 / P2-16）

**Files:**
- Modify: `reporter.py:418-420`（去 astype(str)）、`reporter.py:476/495`（all_ids 排序）、
  `reporter.py:200-207`（涂色范围 2..7）
- Test: `tests/test_review_fixes.py`

- [ ] **Step 1: 失败测试**——汇总 df 数值项为数字类型；同一 matcher 两次构建
  details 行序一致（通过固定排序键断言）。
- [ ] **Step 2-4:** 实现 + 回归。

---

### Task 9: P2 组（matcher 异常记录 / 贪心随机性 / 方向缺失防御 / round_decimal / 死代码）

**Files:**
- Modify: `matcher.py:1146`（except 记 exception_logger）、
  `matcher.py:327-331`（shuffle 后不再 sort）、`matcher.py`（删 `_greedy_fallback`）、
  `data_loader.py:730-731`（方向缺失→全部记失败）、
  `utils.py:293-309`（decimals 参数生效）、死 import 清理
- Test: `tests/test_review_fixes.py`

- [ ] 每项先失败测试后实现；最后全量回归。

---

### Task 10: readme 同步与更新记录

**Files:**
- Modify: `readme.md`（默认参数表述、六级→七级流程说明、第六 sheet 名称、
  严格核销描述、本轮更新记录）
- [ ] 全部测试通过后最后执行；`python -m pytest tests/ -q` 终验。

# 本机默认在线模型实施计划

目标：这台电脑启动GUI即启用用户指定API，个人配置留在本机。
架构：GUI读取现有应用目录的配置，复用build_llm_config及LLMAssistant；不改变业务算法。
技术：现有Python、JSON、customtkinter及API客户端，无新增依赖。
设计：docs/superpowers/specs/2026-09-06-本机默认在线模型-design.md。
授权：用户已明确要求配好并默认启用；沿用本对话的自主完成、README及master提交推送授权，不重复询问相同决定。

## 六阶段

- [x] 需求澄清：新密钥优先、指定服务、默认启用及算法边界明确。
- [x] 方案设计：采用已有本机应用目录与校验函数。
- [x] 实施计划：按下述顺序串行执行，不新建分支或子任务。
- [x] TDD实现。
- [x] 验收审查。
- [x] 交付完成。

## 任务与检查

- [x] 在tests/test_默认大模型配置.py写真实GUI行为测试：有配置时启动启用并传入对话框，关闭后状态更新；缺配置保持关闭；非法JSON、列表结构及超时无效时启动不崩溃且不泄露密钥。先运行并观察当前未加载配置导致失败。
- [x] 仅修改gui.py：导入json，初始化读取LocalProjectStore().root / 大模型默认配置.json，经build_llm_config构造；异常记录固定提示。将原手动配置后的状态刷新提取为_refresh_llm_status并在启动使用。超时校验接受1至600秒。
- [x] 使用本机Python运行上述测试、tests/test_gui_upgrade.py、tests/test_llm_assistant.py，再运行全套pytest；退出码0为通过。
- [x] 本机配置写入最新密钥，仅在配置目录保存；验证模型列表和现有语义接口，使用构造数据。查看真实GUI并确认传递到运行状态的连接一致。
- [x] 更新README及复核报告，核对业务算法文件与起点一致，检查待提交内容没有个人密钥；完成master提交推送并核验远端。

测试解释器：C:\Users\27651\AppData\Local\Programs\Python\Python314\python.exe，参数-B -X utf8 -m pytest -q -p no:cacheprovider，临时测试目录每次单独指定，避免覆盖已有文件。

## 验收记录

609 passed, 5 warnings in 285.76s (0:04:45)；27项定向测试通过。原GUI入口、真实API结构化调用及构造数据核对均通过，报告不含密钥。模型评分与理由矛盾已单独披露在复核报告中，未调整算法或提示词。最终提交推送以master远端核验为准。

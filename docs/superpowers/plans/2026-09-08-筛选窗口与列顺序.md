# 筛选窗口与列顺序实施计划

目标：一次设置筛选条件，清楚区分单笔和整组金额，记住新报告列顺序。
架构：扩充已有筛选后端，增加独立窗口和列偏好模块，在写入报告前应用列顺序。
技术：当前Python、Tk/customtkinter、pandas、openpyxl、本机Excel，无新增依赖。
设计：docs/superpowers/specs/2026-09-08-筛选窗口与列顺序-design.md。
授权：用户批准上述三项改进、README更新、提交推送；在指定仓库现有目录的独立分支串行执行。

## 六阶段

- [x] 需求澄清：已解释具体业务例子并获批准。
- [x] 方案设计：采用既有报告数据和生成链，不搬移已填报单元格。
- [x] 实施计划：如下三项，继承本轮执行授权。
- [x] TDD编码执行。
- [x] 验收审查。
- [ ] 交付完成：源码和文档提交、推送后核验远端。

## 任务1：金额筛选

文件：底稿筛选.py、tests/test_筛选窗口与列顺序.py。
接口：FilterCriteria增加amount_basis、min_amount、max_amount、amount_absolute；_filter_groups可接收原始组成；preview_filter返回数量及中文条件。

- [x] RED：`FilterCriteria(amount_basis='银行单笔', min_amount=10000)`在旧版缺参数失败；工资银行8000与账面800000单笔对照、整组、负数、等额边界、非法值、原报告保护与完整组成。
- [x] GREEN：读取原始明细金额，按事项编号保留整项；导出写明金额口径。
- [x] VERIFY：运行新增测试及tests/test_复核保存筛选.py、tests/test_报告阅读布局.py。

## 任务2：集中窗口与列偏好

文件：新增筛选窗口.py、报告偏好.py；修改gui.py、reporter.py。

- [x] RED：调用缺失窗口及偏好接口；存偏好后重新生成报告，断言列序改变、数据与人工选择有效。
- [x] GREEN：单窗口集中设置条件；后台预览，既有后台导出；列偏好按表名和列名保存，在数据表写入前应用。
- [x] VERIFY：真实Tk窗口交互测试；本机Excel重算、超链接目标、输入源保护和旧配置兼容。

## 任务3：文档、审查、交付

- [x] 完整pytest，后台持续输出阶段进度，复核匹配相关文件无变更。
- [x] 更新README、CONTEXT和审查记录；仅交付源码和文档。
- [ ] 检查最终差异与备份，提交并推送，查询远端提交号核对。

测试命令：指定项目目录使用本机Python314的`-B -X utf8 -m pytest`；通过数量和退出码0为验收证据。本次不打包、不更新桌面EXE。先前超出授权的桌面替换已从备份恢复并校验。

用户追加要求：列顺序只影响展示，不决定字段身份。统一按报告首行列名识别，容忍列换序、额外列和两端空格；重复列名及必要列缺失必须明确提示。Excel移动列并填写人工意见后，预览、筛选、意见、备注和公式结果必须正确，源文件不能被改写。

验收实绩：632项全量回归通过，最终54项筛选与报告专项通过。真实旧报告2281项关系中筛选370项，金额9159728.47元，与独立复算一致；原报告哈希不变。

远端衔接：保留1a08061及其上游修复，整合后675项全量回归通过；真实窗口后台预览与实际选项再筛选通过。桌面EXE已恢复原版并核对指纹。

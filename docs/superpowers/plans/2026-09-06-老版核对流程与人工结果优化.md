# 老版核对流程与人工结果优化实施计划

执行方式：沿用本会话自主执行授权，在原工作分支串行集成；独立文件可分工，文件所有权明确，禁止交叉写入。

目标：在5a069ca上保留核对能力，补齐月度、完整明细、全查抽样和人工结果的实际衔接。
架构：Matcher原计算不重写；复核事项构造报告分流；复核报表使用普通Excel公式；Reporter及原筛选入口集成。
技术：本机Python3.14、现有pandas/openpyxl/Tkinter。原目录：C:\Users\27651\Documents\Code\银行流水核对工具。
设计：docs/superpowers/specs/2026-09-06-老版核对流程与人工结果优化-design.md。

## 全局约束

输入只有银行流水和序时账；不新增依赖/宏/补资料流程；中文UTF-8；既有文件修改前集中备份；不修改master；不删除文件；工作流阶段均留记录。

## 任务1：月度余额取值一致

文件：balance.py、reporter.py；测试：tests/test_月度余额取值.py。
- [x] 红灯：构造同日100→5100、300→5400、200→5600的乱序；断言月末仍5600，差额0；覆盖净额零与真实缺口。
- [x] 最小实现：balance.daily_balance_points(frame)复用_closed_day_balances；reporter每日取值使用该函数，再汇总月份。
- [x] 验证：目标测试与原余额/断档测试通过，输入表不变。

## 任务2：统一全查与抽样事项

文件：复核事项.py；测试：tests/test_复核事项分流.py。
- [x] 红灯：20万元单边、同月21笔5000元单边、20万元明确对应、中风险等距抽取及未抽中不留人工。
- [x] 最小实现：build_review_items(matcher)，沿用候选分级；加入未对应与完整同侧链；单边累计与已有差异池同方向合并后升级；按完整总体等距抽样。
- [x] 验证：每个原记录只有一个初始事项；所有组成与金额正确，不修改matcher。

## 任务3：可直接核对和选择的Excel

文件：复核报表.py、reporter.py；测试：tests/test_复核工作簿.py。
- [x] 红灯：月度、明细、全查、抽样直接可见；人工结果在对应业务旁；原记录完整；旧专题有数据仍可用。
- [x] 最小实现：构造主表和必要的隐藏选项成员表；一个事项一个人工单元格；原始明细、候选组成分开；同组成员默认展开。
- [x] 红灯：确认差额不消差、否定取消有效对应、替代候选不双重占用、人工冲突提示、月度解释加总与全量一致。
- [x] 最小实现：使用SUMIF/COUNTIF/INDEX/MATCH等Excel普通公式，让每笔原记录的当前去向、覆盖和月度解释引用有效选项。
- [x] 验证：openpyxl结构检查与本机Excel实际计算；筛选排序保存后身份不串行。

## 任务4：保存重开及筛选

文件：项目记录.py、gui.py、底稿筛选.py；测试：tests/test_复核保存筛选.py。
- [x] 红灯：已填人工结果保存后仍从原入口打开；新筛选保留完整组成、人工选择及公式；原文件哈希不变；硬链接拒绝覆盖。
- [x] 最小实现：保留来源指纹，重开允许内容变更；筛选按事项稳定身份隐藏非选中完整组，保留原表全部公式引用；旧格式维持兼容。
- [x] 验证：真实Excel读取、筛选、重开和既有历史筛选测试。

## 任务5：文档、验收和交付

文件：readme.md、CONTEXT.md、docs/reviews/2026-09-06-老版核对流程与人工结果优化-review.md。
- [x] 同步术语、操作说明、分流口径和功能变化；不得写成EXE已更新。
- [x] 完整测试命令：指定本机解释器 -B -X utf8 -m pytest -q --tb=short -p no:cacheprovider。成功输出为全通过，保留原日期警告说明。
- [x] 主入口构造样例、Excel渲染与人工选择计算、原数据哈希校验、语法及git diff检查。
- [x] outputs交付示例和验收报告。
- [x] 按已授权提交、推送、核实远端同一提交；实现提交1a775aeac9c7c8927c53682b41b9270b345918c9。

## 实际验证

本轮最终全套：588 passed，5项原日期解析警告，耗时245.42秒。真实Excel五项综合行为测试覆盖确认、差额、否定、替代、冲突、排序、非法粘贴及空串计数。真实桌面入口2.17秒生成样例，双击入口自检READY；源文件哈希不变。主入口四个月23笔样张、1000组2000行报告层性能检查通过，详见本轮独立复核报告。

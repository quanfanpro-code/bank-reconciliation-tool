# 疑点分层处置实施计划

日期：2026-09-06
对应设计：docs/superpowers/specs/2026-09-06-疑点分层处置-design.md

## Task 1 备份

备份 matching_policy.py、matcher.py、reporter.py、CONTEXT.md、readme.md 及将受影响的旧测试文件到 `C:\Users\27651\BackUp\银行流水核对工具_20260906_<时分秒>\`。

## Task 2 失败测试先行（红灯）

新建 `tests/test_疑点分层处置.py`，按设计第四节 6 个测试点编写，运行确认全部失败（函数不存在/行为未实现）。造数模式复用 `tests/test_余额断档局部降级.py` 的 `_余额行`/`_写带余额表` 与 `tests/test_独立复核修复.py` 的 `_行`/`_写表`。

## Task 3 实现 route_candidate 放宽分支

matching_policy.py `route_candidate`：原因分弱证据/硬冲突两组；在 GROUP_RECONCILED 之后、差异分支之前插入明显微小自动确认分支。

## Task 4 实现 apply_medium_risk_sampling

matching_policy.py 新增函数；matcher.py 在差异池回填循环后调用并存 `self.medium_sampling_stats`；data_structures.py 如需在 Matcher 上声明该属性则补一行初始化。

## Task 5 报告侧

reporter.py `_build_issue_table` 按 `evidence["medium_sampling"]` 改建议动作、判断依据追加抽样标记；总览统计区新增"中风险抽样总数""中风险抽中待核查数"两行（缺省不显示）。

## Task 6 旧断言适配

运行全量测试，逐个核实因 R1 取代规则而失败的旧测试（预计 test_reliability_upgrades.py、test_automation_first.py 中候选歧义/业务依据不足相关），按新口径调整（金额调至临界值之上验证旧规则仍挡，或改断言新行为），记录到审查报告。

## Task 7 真实数据验证

运行 `C:\Users\27651\AppData\Local\Temp\real_e2e_run.py`（约 7 分钟），核对：低风险疑点大降、中风险仅抽中部分标需人工、未抽中仍列示、截止性差异表行数不减少、范围未知 19 笔不变。

## Task 8 验收审查

写 `docs/reviews/2026-09-06-疑点分层处置-review.md`：范围、发现、旧断言调整清单、验证证据、残余风险。写完补 BOM。

## Task 9 交付

更新 readme.md（疑点分层处置条目）与 CONTEXT.md（"明显微小错报临界值"词条按新口径改写，新增"中风险等距抽样"词条；相关 md 补 BOM）；全量测试；commit + push（用户已授权）。

# 个人独立EXE需求

用户明确要求：打包为独立运行的Windows EXE，内置指定在线API、Qwen3.8-27B及本轮最新密钥，GUI默认启用，成品放桌面，完成后清理本次打包临时文件。仅供本人使用。

范围为现有项目master；延续自主完成和README、提交推送授权，不创建分支。只修改打包和配置加载，不改核对、评分或提示词。

验收：只复制EXE、使用空白应用配置及不含Python的PATH仍能启动真实GUI，默认启用指定模型；验证内置密钥匹配但不输出；真实连接测试成功；构造数据报告生成；适用测试通过；桌面文件哈希一致；临时文件列清单移入回收站。

保密边界：个人密钥内置意味着EXE可被提取；HTTP接口没有HTTPS传输加密。已向用户说明，用户确认本人使用。含密钥EXE只本地交付，源码仓库只提交通用读取与打包逻辑，不提交密钥或EXE。

研究：2026-09-06核实本机PyInstaller 6.22.2。官方运行时文档说明__file__可定位包内数据；单文件模式自带解释器，启动解包到临时目录，正常退出后清理。
来源：https://pyinstaller.org/en/stable/runtime-information.html
https://pyinstaller.org/en/stable/operating-mode.html

待确认项：无业务或配置歧义。用户再次明确授权完成后清理本次临时文件；仅处理集中创建的打包目录，保留备份、原配置、原有文件和成品。

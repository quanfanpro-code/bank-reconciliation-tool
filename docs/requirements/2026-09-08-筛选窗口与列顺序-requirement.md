# 筛选窗口与列顺序需求

用户已在解释单窗口、单笔与整组金额、列顺序保存之后批准“改吧，更新README，提交和推送”。沿用该批准，串行实施，不重复提问。

对象：单账户银行流水及对应银行存款序时账的核对报告。解决连续八次弹窗、筛选金额含义不清、常用列顺序不能记住的问题。

范围：一个集中筛选窗口；银行单笔、序时账单笔、整组金额的含边界上下限；金额默认取绝对值，可使用统一收正支负的带符号金额；选中任何原始组成即保留完整事项；保留人工结果、原公式和原报告。金额不足以从旧报告识别时明确报错，不能默默当零。

列偏好在本机保存，应用于以后生成的新报告。当前报告筛选仍保留现有列位置；避免搬移已有人工结果或公式。固定保留证据列，只改排列，不删列。匹配算法、重要性分层和抽样规则不变。

验收：工资100笔各8000元与账面800000元，银行单笔与序时账单笔应得到不同命中结果；整组筛选完整带出双方；负数、边界、空范围、银行独有和非法条件正确；列顺序重新生成后生效，Excel人工选择、公式与超链接正常；全量测试通过；README、提交和远端一致。

调研依据：本任务v1.0.1.3反编译报告。借鉴集中配置、列偏好，不复制对方算法和默认排除条件。

待确认事项：无。用户已授权本范围实施和提交推送。

用户追加要求：列顺序只影响展示，不决定字段身份。统一按报告首行列名识别，容忍列换序、额外列和两端空格；重复列名及必要列缺失必须明确提示。Excel移动列并填写人工意见后，预览、筛选、意见、备注和公式结果必须正确，源文件不能被改写。

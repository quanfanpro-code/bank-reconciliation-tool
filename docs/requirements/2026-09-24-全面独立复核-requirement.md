# 2026-09-24 全面独立复核 需求

## 背景与问题

用户要求对银行流水核对工具当前版本（基线 c25191c，含非工作日银行收付示警）做一次全面独立复核，完成后同步更新 README 并提交推送。上一次全面独立复核在 2026-09-07，此后经历了筛选窗口与列识别、筛选交互易用性、非工作日示警三轮变更。

## 用户与使用场景

项目维护者本人。复核用于确认当前版本全量测试、关键链路和最新功能在真实使用前的可靠性，复核结论写入 README 供使用者参考。

## 功能范围

1. 全量自动测试的新鲜运行证据（pytest 全套）。
2. 关键链路静态审查：候选生成与选择（matcher.py）、评分与重要性分流（matching_policy.py）、输入读取与十三项检查（data_loader.py、input_precheck.py）、人工结果与覆盖率（复核事项.py、复核报表.py、报告展示.py）、筛选导出（底稿筛选.py）、最新非工作日示警功能（非工作日日历.py 及报告集成）。
3. 对审查中发现的疑点构造边界用例验证。
4. 发现的缺陷按 TDD 当场修复（先失败测试，再最小修复）。
5. 复核报告写入 `docs/reviews/2026-09-24-全面独立复核-review.md`。
6. 同步更新 README（更新日志、验证数字与实际情况不符处）。
7. 提交并推送到 origin/master。

## 明确不做的内容

- 不做真实银行流水端到端验证（无真实资料，以构造数据为准，报告如实标注）。
- 不重新打包个人 EXE。
- 不做与本次复核无关的重构。
- 不改动匹配算法规则本身，除非复核证实其存在缺陷。

## 约束与风险

- 宿主规则：修改已有文件前先备份到 `C:\Users\27651\BackUp\`；禁止 rm -rf；中文文件 UTF-8（中文文本优先 BOM）；不擅自扩大范围。
- matcher.py、reporter.py 体量大，静态审查按关键链路抽查，无法逐行覆盖，复核报告如实说明覆盖范围。
- GUI 部分以代码审查和既有 GUI 测试为准，不做人工点击验收。

## 验收标准

1. 全量 pytest 通过（含新增边界用例），记录退出码和通过数。
2. 审查发现的问题逐项有结论：已修复（附测试证据）或确认为非问题（附理由）。
3. 复核报告完整记录范围、发现、修复、验证证据和残余风险。
4. README 更新日志与实际验证数字一致。
5. 提交推送成功，远端与本地一致。

## 调研摘要与来源

- 仓库：`C:\Users\27651\Documents\Code\银行流水核对工具`，已确认与 origin/master 同步（c25191c）。
- 本机 Python `C:\Users\27651\AppData\Local\Programs\Python\Python314\python.exe`，依赖齐全。

## 待确认项

无（用户已明确范围：全面独立复核、发现即修复、构造数据验证、完成后更新 README 并提交推送）。

# 老版核对流程与人工结果优化需求

基线：5a069cafa65fe7ed3f92539b0ba1e4c5333a27d8。恢复提交 fc5ef9a，与基线文件树完全一致，537项测试通过，已推送。

## 已确认目标与授权

用户先要求完整解释核对目的、实际源码流程与各种业务情况，随后明确回复“基本就这样，先回退到5a069ca，然后用上面的思路需求优先工作流开干”。沿用本会话“后续流程自主完成、README同步、提交推送”的授权，需求、方案及计划在下述边界内自主落实，不重复询问已回答事项。

输入只有一个账户的银行流水及对应序时账。核对收付款双向对应、全量月度收支和余额，解释具体差异；不收集额外资料，不编制账务调整。只优化指定项目，保留原有业务组、组合、退款重付、手续费、重复及截止性识别。

## 决策树与业务场景

1. 目标：不是追求工作表少，而是完整信息可读、人工只选择与具体业务相符的结果。已由用户确认。
2. 资料：仅两份输入；原件不变；输入缺口客观披露，不新增补材料任务。已确认。
3. 对应：逐笔、完整组、同业务金额差、期间差、竞争候选、单边及同侧退款链分开；不能同额凑平，不虚构逐笔关系。代码与样例已核实。
4. 分流：沿用已选关系的重要性口径及先累计后等距抽样规则，补齐单边记录的分级和累计，使全查及抽样列表覆盖完整总体。大额明确对应仍自动确认，未抽中不计待办。范围受限沿用现行限制，不另造定性高风险规则。
5. 人工：确认对应（有金额差时保留差额）、否定对应、选择已有其他候选、保留单边结果。一次业务填写一次，不设需调整、待补充资料、责任人或处理日期。结果必须作用于最终对应及覆盖率，而非只变进度。
6. 月度：保留双方全量收入、支出、余额，按原始年月分解各事项贡献；原始数不随人工判断改变。已解释与未确认贡献分开，能够回查记录。
7. 使用：Excel中选择和保存即可；无宏、无新增依赖、无额外导入步骤。修改后的报告可重开，筛选版另存并保留人工结果。
8. 交付：固定样例自动验证与Excel实际计算、显示检查，完整测试，README同步并推送现有分支，不合并master。

## 验收标准

- 月度末日乱序：两侧真实5600元，月度均为5600，差额0；真实断档仍保留。
- 每个原始有效记录在核对明细中出现一次，源金额有符号可加总；替代候选不重复计为原始总体。
- 20万元明确同业务同额仍自动确认；20万元单边进入全查；中风险按日期等距抽取，未抽中及低风险不成为待处理。
- 月度单边累计超过重要性时组成全部升级；不跨年月或收支方向抵销。
- 600元2对3完整组成保留；跨年3000元对应仍有期间差；手续费原始收支和关系公式同时保留。
- 确认有差额关系不把差额变0；否定对应取消其有效覆盖；选择其他候选更新有效组成；重复使用原记录的选择明确阻止计入。
- 人工选择只在全查或抽样表一处填写，明细、月度解释、首页自动引用；排序、筛选、保存重开不串行。
- 不出现需调整或补资料操作；空人工结果不等于未运行完成。
- 技术字段可隐藏，月度、全量明细、两类人工表和需要的业务辅助信息可直接访问。

## 明确不做

不重写匹配搜索算法、不改变已有强证据门槛、不以大小额替代业务身份、不新增多账户或多文件合并、不编制银行余额调节表、不编制会计调整、不安装依赖、不修改客户源文件。本轮交付源码及示例，不重打包EXE。

## 依据与风险

依据为本会话用户指令、5a069ca源码及六个内存样例；没有使用外部审计规则替代项目约定。复杂竞争最多沿用老版已保留的10套替代组成；搜索未穷尽继续披露。人工判断不推断材料之外的原因。Excel计算需用实际桌面Excel验收，不能只靠openpyxl公式文本检查。

## 六阶段进度

- [x] 阶段1：需求澄清，用户已确认业务梳理和执行范围。
- [x] 阶段2：方案设计。
- [x] 阶段3：实施计划。
- [x] 阶段4：TDD编码。
- [x] 阶段5：验收审查。
- [x] 阶段6：样例和复核报告交付；实现提交1a775aeac9c7c8927c53682b41b9270b345918c9已推送，远端同一分支SHA核验一致。

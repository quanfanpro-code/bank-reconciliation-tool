# 匹配可靠性与交付补强复核记录

## 复核范围

- 冗余清理：旧报告器、旧倒排索引、旧精确/容差 worker、旧白名单直写链路、无调用结果类型和重复表头入口。
- 可靠性：关键文字字段冲突、候选歧义、连续摘要整组候选及其统一评分和分流。
- 交付：`requirements.txt`、Windows 安装与启动说明、README 功能边界。
- 复核基线：`d4584968`；本轮实现提交截至 `1d1401d`。

## 需求逐项结论

| 需求 | 结论 | 证据 |
| --- | --- | --- |
| 先清理确认冗余 | 通过 | 清理提交删除 1,136 行、增加 41 行；清理后 193 项测试通过 |
| 冲突或歧义不得自动确认 | 通过 | 分流共同入口在金额一致和明显微小规则之前拦截，并披露全部原因 |
| 双向连续摘要整组 | 通过 | 银行侧和日记账侧均可成组；使用原表行号、日期窗口和收支分别一致约束 |
| 进入现有候选管线 | 通过 | 新候选只调用 `_add_candidate`，继续经过评分、歧义识别、非冲突选择和风险分流 |
| Python 自包含交付 | 通过 | 依赖清单可解析；README 包含安装和启动命令，不需要 Excel 插件或宏 |
| 单银行流水对单银行日记账 | 通过 | 未增加多银行合并、批量账户合并或旁路输入结构 |

## 代码复核发现与闭环

1. 连续摘要候选的报告类型最初会显示内部英文名。已补充“连续摘要整组”中文映射并增加测试。
2. 连续摘要识别最初默认 `original_idx` 一定存在。已改为缺列时安全跳过、异常行号中断连续组，不用清洗后索引替代。
3. 初版测试尚未覆盖明显微小差异、单独歧义、空摘要、摘要不同和日期超窗。已补齐全部计划边界，并增加缺少可靠原表行号的防回归测试。
4. 冗余清理后仍残留无调用的 RapidFuzz 可选导入。已删除，不把无实际用途的包写入依赖清单。

代码复核未发现未关闭的严重、较高或一般问题。Ponytail 复核结论为新增能力已复用现有入口，无需增加新配置、领域对象、依赖或旁路报告结构。

## 实际验证结果

- 全量测试：`206 passed in 18.63s`。
- 编译检查：`python -X utf8 -m compileall -q .`，退出码 0。
- GUI 隐藏启动：窗口和日志组件创建、更新并销毁成功。
- 端到端报告：成功生成 12 个工作表；包含“输入预检查”“匹配明细”“匹配组成明细”；连续摘要整组为自动确认，处理原因为“金额完全一致”，组成明细共 3 行。
- 依赖解析：`python -X utf8 -m pip install --dry-run -r requirements.txt` 成功，未安装新包。
- 编码与空白：本轮中文文件可按 UTF-8 严格读取，未发现 Unicode 替换字符；`git diff --check` 通过。

## 残余边界

- 连续摘要整组仍是候选证据，不是无条件结论；若出现关键文字冲突或多个去向，会进入人工复核或由更可靠候选取代。
- `requirements.txt` 使用兼容版本区间，不提供锁定环境、安装器或自动联网安装脚本，这是本轮明确的轻量交付边界。
- 本工具继续只处理一份银行流水和与其对应的一份银行日记账，不合并不同银行或账户。

## 待交付动作

- 完成最终新鲜全量验证。
- 正常推送 `master`，并核对云端提交号与本地一致。

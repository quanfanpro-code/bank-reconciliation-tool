# 老版核对流程与人工结果优化复核报告

日期：2026-09-06。项目：C:\Users\27651\Documents\Code\银行流水核对工具。

## 结论与范围

以5a069ca原文件树为基础完成报告及人工操作优化；恢复提交fc5ef9a已推送，保留历史。本轮未重写Matcher搜索算法，未改变单账户两份输入边界，未重打包EXE。最终源码测试588项通过，原桌面入口和本机Excel计算通过。已准备提交当前工作分支；远端回执在推送后补记。

六张日常主表为核对结论、月度核对、核对明细、人工全查、人工抽样、每日统计。退款冲销重付、手续费及净额、截止性差异、重复线索及余额异常等专题有数据时显示。旧重复表、机器参数、候选与计算表保留为隐藏表。没有为减少张数而丢弃原业务信息。

## 发现及修复

1. 月末按物理末行取余额可能错取同日乱序记录：复用原余额链取真实每日期末；实际交易日缺余额不补成0，不用前日余额掩盖缺口。真实日界断档继续保留。
2. 单边记录原来未纳入完整分级及抽样：补齐原始事项唯一归属，按自然年月、来源、收支方向累计，再在完整总体中全查及等距抽样；原已明确对应的大额不重复推给人工。
3. 原报告后续状态不能改变实际关系：人工可确认、保留差额、否定或采用已有其他对应；单边可保留未对应。选择结果更新有效关系、原记录去向、月度解释和已对平覆盖，不设资料、调整、责任人或日期流程。
4. 人工替代释放的原记录曾误显示已确认，单边被其他关系采用又可能显示未对应：现逐笔按有效对应判定；冲突关系全部退出有效覆盖，原记录不重复计数。
5. 月度差异原按初始组固定且排除零差组：保留每月原所属事项的全部组成，由当前有效原行拆分已确认和未确认贡献，原始月度金额与余额不改写。
6. 真实Excel空单元格与公式空字符串计数存在差别：修正INDEX空白变0、非法选择漏计待核对、空串误计已对应等；分别取得失败与修复后实际计算证据。
7. 原人工组成行没有独立归属，排序后可能散失：主表显示短核对编号，组成行重复归属，隐藏填报编号只定位唯一填写行；实际Excel降序排序及筛选后仍对应原事项。
8. 保存人工结果后历史指纹阻止重开：仅最近报告入口允许内容变化，保留原生成指纹与默认严格校验接口。
9. 新报告筛选若经表格重写会丢公式及人工结果：采用原簿另存筛选视图，仅隐藏非选中完整组，所有公式与候选上下文保留；全量月度/首页口径明示。硬链接别名也禁止覆盖原报告。
10. 保留原期初余额警告高亮、公式注入防护和源文件保护。新原始出处提供文件名、工作表及行号；旧后续状态的测试按用户已批准的实际核对结果口径更新。

## 验证证据

- 最终全套：指定本机Python3.14执行`-B -X utf8 -m pytest -q --tb=short -p no:cacheprovider`，588 passed，5 warnings，245.42秒。警告来自原日期解析测试，不是新增失败。
- Excel五项综合测试：未填、确认100对90且保留10元差额、否定、B1/J1改选B1/J2释放J1、重叠冲突、单边被采用、整行排序、非法粘贴、公式空串计数；全部读取CalculateFullRebuild实际结果。
- 保存筛选10项回归及最近报告重开测试：真实人工填写、公式、备注、完整组成和原报告SHA256均核验。
- 实际应用入口构造样张：四个月，银行13笔、序时账10笔；2项全查、2项抽样；23笔原始行唯一完整；月度余额衔接差均为0，错误公式0；源文件哈希不变。已检查本机Excel导出的主表页面。
- 原main.py进入Tk事件循环，调用原开始按钮处理链：2.17秒生成报告；原双击入口自检READY；源文件哈希不变，历史正常保存。
- 1000组报告层压力测试：真实单组匹配模板延展，2000笔原行，构表0.808秒，写入排版57.381秒，Excel打开0.967秒，完整重算0.136秒；两侧已对平覆盖100%、未对应0、错误公式0。源码指纹前后相同。
- 本轮修改前集中备份及SHA256核验；中文UTF-8回读、语法检查和Git差异检查通过。

## 使用与边界

黄色单元格是主要人工操作位置，备注自愿。已人工查看与已对平不同：确认仍有差额、否定或保留未对应，都不会将客观差额改为0。月度原始数不随人工选择改变。

筛选文件是包含完整计算上下文的筛选视图，非选中行可以取消隐藏；首页与月度仍为全量范围，不是只含样本的删行版。正文已明确该口径。

本次交付源码与构造样例，不声称客户真实业务全部核对无误，也不以报告层压力测试证明匹配算法准确率。原有搜索预算/超大竞争组降级仍按原程序披露。Windows双击源码入口采用本轮修改，既有EXE未重打包。

## 本地验收记录

- 主入口、Excel计算与页面：C:\Users\27651\Documents\Codex\2026-09-06\c-users-27651-documents-code\work\老版优化验收_175904\验收结果.json
- 真实桌面入口：C:\Users\27651\Documents\Codex\2026-09-06\c-users-27651-documents-code\work\老版桌面验收_180033\验收结果.json
- 压力测试：C:\Users\27651\Documents\Codex\2026-09-06\c-users-27651-documents-code\work\千组报告性能验收_20260906_180105_572739\计时证据.json
- 交付样例：C:\Users\27651\Documents\Codex\2026-09-06\c-users-27651-documents-code\outputs\银行流水核对报告-老版优化样例.xlsx
- 筛选视图：C:\Users\27651\Documents\Codex\2026-09-06\c-users-27651-documents-code\outputs\银行流水核对报告-老版优化筛选视图.xlsx

# 2026年9月7日全面独立复核与边界修复

## 一、本轮范围和授权

本轮按用户指定的“需求优先工作流第五步”对当前程序独立取证、构造反例、修复及重新验收。起点为 `ff31b6d0c9515f2c403d50bf44324106bf19fa87`，本地工作区起始干净。仓库实际主分支为 `master`；用户已明确确认沿用现有 `master` 提交推送，不建立新分支。

复核依据为现有单账户业务需求、输入预检查要求、余额局部降级、疑点分层、老版流程与人工结果优化及报告展示要求。沿用已经确定的重要性、抽样、人工选择口径，不自行新增会计判断。9月6日“只改展示”的限制属于当时改版范围，本轮按新的全面复核授权修复已复现的业务及数据保护问题。

### 六阶段状态

1. 需求澄清：承接仓库已有确认需求及本轮第五阶段复核指令。
2. 方案设计：承接现有设计，保持单账户、两份输入及审计证据保留方式。
3. 实施计划：先查证和失败复现，再最小修复、回归、实开验收、更新文档及原分支交付。
4. 编码执行：仅修复下表中的已复现错误；每组修复先取得失败证据，新增43项行为检查。
5. 验收审查：已完成下述领域复核，最终655项全量验证通过，详细结果见第五节。
6. 交付完成：按用户已确认的 `master` 提交推送；不新建分支，不重新打包个人EXE。

修改前，146个已跟踪文件已逐个备份并校验SHA256；补充修改前也保存了相应中间版本。备份及本地验证证据位于 `C:\Users\27651\BackUp\银行流水核对工具_20260907_141350\`。真实客户资料、模型密钥、测试Excel和个人EXE不进入本次提交。

## 二、审查覆盖与方法

| 领域 | 本次取证和检查 |
| --- | --- |
| 输入和范围控制 | 文件读取、表头、账户币种、金额口径、原行归属、金额方向、缺失与解析异常、余额连续性和范围降级 |
| 业务对应 | 逐笔与组合候选、编号与对方冲突、退款冲销重付、费用净额、零金额、原行唯一占用及不重叠关系选择 |
| 分流与人工结果 | 金额及月份差异、重要性边界、人工全查和抽样、人工选择冲突、替代关系、原值与月度结果保留 |
| 输出与数据保护 | 同名文件、失败半成品、运行中取消、筛选另存、长账号、原始数值日期、公式和错误单元格 |
| 模型和桌面入口 | 外部回答结构异常、本地规则降级、参数拒绝、取消信号、真实桌面开始入口和历史保存 |
| 性能与维护 | 现有搜索限额、竞争组降级披露、完整测试耗时、后台任务、变更范围及依赖复用 |

先查实现与需求，再构造独立边界样例；不把旧README中的测试数字当作本轮通过证据。没有启动其他代理，也没有复用此前作者的“已完成”结论作为验收结论。审查覆盖主要处理链及关键分支，不声称每个外部环境和全部输入组合均已穷尽。

## 三、已复现并修复的问题

| 编号 | 程度 | 复现条件与原影响 | 修复后的行为 |
| --- | --- | --- | --- |
| R01 | 高 | 三种金额模式遇到无穷大、超大Decimal或NaN形式字符串，可能整批异常或漏记坏行 | 正常行继续；坏金额按来源和原行留在异常记录中 |
| R02 | 中 | `20260102.0` 数值或字符串日期无法解析 | 去掉八位日期后无意义的小数零，正确识别为2026年1月2日 |
| R03 | 中 | 非有限重要性、内存参数或畸形整数穿过检查或抛异常 | 界面明确拒绝非法参数，不进入正式运行 |
| R04 | 中 | 模型回答的列表字段为null或错误类型时异常逃出解析 | 纳入受控失败及本地规则回退，不确认虚构候选 |
| R05 | 中 | CSV或Excel中的NA、NULL被通用读取器当作空值 | 读取和预览保留原文，真正空单元格仍按空值处理 |
| R06 | 高 | 输出路径已经存在时可能覆盖人工填写底稿 | 正式核对、筛选另存拒绝覆盖 |
| R07 | 高 | 报告初次保存之后排版失败，留下正式半成品或破坏旧文件 | 同盘临时写入完成后才交付；失败不影响已有文件 |
| R08 | 高 | 读取阶段没有匹配器，点击停止不起作用 | 独立取消信号覆盖读取及后续阶段，不继续交付报告 |
| R09 | 高 | 匹配完成或报告写入期间收到取消，仍保存成功报告 | 匹配后及正式交付前检查取消，不保存历史成功记录 |
| R10 | 中 | 筛选比例越界或NaN被悄悄截成全选或空选 | 公开入口拒绝0至1范围以外及非有限比例 |
| R11 | 高 | 筛选保存中途失败可能留下看似正式的损坏底稿 | 筛选版同样完整写入后交付，全量报告保持不变 |
| R12 | 高 | 设备款-1000与手续费-10合并为-1010时，因同为支出而漏掉费用关系 | 支持付款加费用及收款扣费用，保留费用文字、共同业务和金额方向约束 |
| R13 | 高 | 同一对方但业务编号明确不同，或同一编号但对方冲突，被串成退款链 | 明确冲突不能形成已识别退款链；逐笔回单号不同不机械当作业务冲突 |
| R14 | 高 | 付款—退回—重付—再次退回—再次重付时，中间记录进入两条链 | 已占用原行不再次进入识别链；证据不足部分仍保留待查 |
| R15 | 中 | 两笔零金额被当成正负同额退款线索 | 零金额不制造此类线索，原行仍保留 |
| R16 | 高 | 开始时目标不存在，但运行期间出现同名完成底稿，最后保存将其覆盖 | 使用Windows拒绝覆盖的改名操作；即使晚到的同名文件也受保护 |
| R17 | 高 | 原日期20260105被报告排版变为错误单元格，年份2026和月份1被显示成其他日期 | 数字保持原值；真正日期对象继续按日期展示；生成与再次美化均覆盖 |

R01—R05对应21项独立用例，初次均复现失败。文件与取消的初始9项均复现失败，修复后通过。业务边界6项均复现失败，修复后连同8项原有业务检查共14项通过。补充的取消、保存故障、运行中同名文件和日期排版共7项中，4项复现新增失败，3项为已有修复及正常日期的正向保护。最终新增用例合计43项，原测试断言未修改。

## 四、最小修复与未扩大范围

实际修改12个生产文件，增加3个测试文件，更新README与本记录。数据读取、金额处理和输出保护尽量在公共入口修正，报告生成复用原有构表及排版流程；临时写入仅用Python标准库，没有新增运行依赖，没有改写整套匹配架构。

同侧退款链中的共同高层批次号优先于较低层业务编号，明确对方冲突仍拒绝；没有把任意正负同额都当成退款。手续费只有费用为支出、与主款存在共同业务证据、净额不反向时才形成专用候选。

另检查到每日余额重算内部结构的收入/支出字段按净额拆分；现有报告调用实际只消费净额和余额，月度及每日收入支出由原交易分别计算，因此本轮未把这个未被使用的内部字段扩展为新改造。

## 五、验证结果

### 自动检查

- 起始基线：612项通过，5条日期推断提醒，耗时542.86秒，退出码0。
- 首轮关联回归：79项通过，耗时20.23秒，退出码0。
- 第一轮修复全量：648项通过，5条日期推断提醒，耗时652.10秒，退出码0。此轮发生在补充同名文件竞争和原始数值日期修复前，只作为中间证据。
- 补充文件与日期边界：16项通过，耗时9.94秒，退出码0。
- 最终全量回归：655项通过，0项失败或跳过，5条原有日期推断提醒，耗时775.86秒，退出码0。包含5项由本机Excel实际计算的人工结果测试。完整输出留存于备份验证目录的“最终655项通过证据.log”。
- 源码入口自检：输出 `READY`，退出码0。
- 89个Python生产及测试文件：语法解析、UTF-8读取和替换字符检查通过。

执行环境为本机 `C:\Users\27651\AppData\Local\Programs\Python\Python314\python.exe`，测试目录为仓库 `tests`，关闭pytest缓存。最终全量使用独立目录 `C:\Users\27651\BackUp\银行流水核对工具_20260907_141350\验证\最终全量回归\`，不复用旧测试临时文件。

### 真实桌面入口及Excel实开

使用本机桌面程序真实窗口及“开始”处理入口，设置单账户合成银行3行、序时账4行，覆盖付款手续费、跨月回款、普通付款、数值日期、NA编号和24位带前导零账号。测试时明确关闭在线模型，历史记录只写入验收目录。

- 完整生成31张表，全量7条原始交易均保留；默认7张可见工作表符合现有阅读方式。
- 付款-1000和费用-10与对侧-1010生成1个手续费候选。
- 另存70%金额覆盖筛选版，共32张表，保留全量计算上下文。
- 用独立Microsoft Excel进程分别只读打开全量报告及筛选版，完整重算；各179个公式，无公式错误或常量错误单元格。
- 月度金额另按独立预期逐项核对：1月银行收入2000、序时账收入0，2月银行收入0、序时账收入2000；两侧1月支出均1010、2月均300。全量报告与筛选版均一致，跨月差额保留。
- 长账号保持文本；原始数值日期20260105保持原值，未变成错误日期。
- 两份源文件运行前后SHA256一致；筛选前后全量报告SHA256一致。

第一次实开正是发现R17的证据；修复后在全新的验收目录重新生成并实开，以上结果来自修复后的产物。本地证据为 `C:\Users\27651\BackUp\银行流水核对工具_20260907_141350\验证\桌面实开最终验收\验收结果.json`。

## 六、结论边界

本次已复现的问题按上表修复，655项最终测试全部通过，未留下本轮已复现而未修复的阻断问题。最终验收以自动测试、桌面真实开始入口和Excel实开计算为证；没有做桌面布局逐像素视觉验收，也没有重打包个人独立EXE。

1. 本次没有真实客户资料的人工完成答案，不能报告真实业务准确率或宣称所有合理对应已经穷尽。旧报告中的真实规模结果只是历史记录。
2. 超大竞争组仍按现有策略确定性降级，报告继续披露搜索受限；本次不移除搜索限额。
3. 停止在当前读写或网络调用结束后的检查点生效，不承诺点击后立即强制中断。正式文件已经完成交付后点击停止，不撤销已完成结果。
4. 退款目前按现有两段、三段证据链识别；更长的反复退回链不会重复占用原行，但不能据此宣称所有长链自动完整还原。
5. NA、NULL读取原文保留不等于把这些词一概当成强业务编号；下游的缺失语义及证据门槛仍适用。
6. 本次源码修复不会自动更新桌面旧EXE或既有报告。用户需要重新生成报告才能取得新行为。

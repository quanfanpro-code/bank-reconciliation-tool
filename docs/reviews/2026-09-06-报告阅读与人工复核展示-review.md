# 报告展示改版独立复核报告

复核日期：2026年9月6日。

## 复核结论与改动边界

本轮报告展示改版通过独立复核。已发现的展示问题均已修正，最终全量测试通过，未发现尚未关闭的确定性阻断问题。后端算法没有改动，也没有提出需要改算法才能完成的事项。

本轮对象为 C:\Users\27651\Documents\Code\银行流水核对工具，工作分支为 codex/single-account-business-20260905，起点为 d23bd6d6a335dea3e948f4bb984e736c6f0544f1。该分支此前已按5a069ca恢复并继续优化；本报告的“不改后端”比较本轮开始与结束，不把历史版本的其他变更算成本轮成果。

本轮仅新增报告展示组织与格式，接入既有报告出口，适配筛选后的可见工作表，并修改相关测试、README与说明文档。20个原有核心文件逐字节校验一致，包括匹配、金额与余额、重要性、抽样、输入解析、人工选择生效规则及桌面入口。既有人工计算表和公式保留。

## 实际阅读和操作

默认显示7张表，其他原业务专题与计算表隐藏保留：

| 工作表 | 查看和操作方式 |
| --- | --- |
| 核对结论 | 看核对范围、覆盖情况及人工核对完成情况。 |
| 月度核对 | 每月双方收入、支出、差额及月末余额在同一行；全年发生额合计在表尾，余额不相加。 |
| 逐笔核对 | 一对一双方并排，日期、摘要、对方或凭证、金额、差额与当前结果直接比较。 |
| 整组核对 | 先看日期范围、摘要、双方笔数、分开的收支合计与差额；点左侧加号展开，先列银行原记录，再列序时账原记录。 |
| 人工全查 | 沿用原入选规则；组成默认展开，每项主行一个黄色选择格。 |
| 人工抽样 | 沿用原抽样范围和样本；操作与全查一致。 |
| 未对应记录 | 保留初始单边记录与原文件出处，显示人工选择后的当前去向。 |

人工主要动作仍是选择一次核对结果并保存。下拉沿用原选项：按事项显示“确认对应”“确认对应并保留差额”“否定对应”“保留未对应”，有既有候选时可选择“采用其他对应”。不增加补资料、账务调整、导入人工结果、负责人或处理日期。

整组汇总的金额写在带有“收入、支出”标签的文字中；组成金额只在原始记录的数值列出现。替代候选的金额也用文字说明，避免求和时把主汇总和替代方案再次加进去。银行与账面组成分开排列，不暗示多对多已确定逐笔配对。

## 独立复核发现与处理

已修正空数据报告的月度排版错误；空报告只有表头，不制造零余额或年度发生额。

已修正人工否定、采用其他对应后，旧展示仍声称原记录全部未对应的问题。逐笔和组成行显示当前对应编号；原整组主行提示展开查看各笔去向，人工一对一主行提示到逐笔表查看。未选择的候选不再冒用当前事项的“已确认”结论。原初始分组位置保留，不重建匹配关系。

核对编号的跳转改为按事项查找唯一填写格。真实Excel整行排序后，Excel计算的跳转目标仍指向该事项。此项验证的是实际公式计算出的目标，不冒称已模拟鼠标点击每个链接。

同时检查了完整原始记录、两侧组成分块、金额不重复加总、月度跨年与空余额、整组筛选后的记录及公式保留、输入文字导出防护。独立复核与实施由不同执行者完成；复核通过时报告展示文件指纹为 B22C76DEB8BA0398B6F9932BB2ADDB18BB0E75C341198004ED4DC4E88E82C267。

## 最终验收证据

- 冻结代码完整测试：604 passed, 5 warnings in 279.75s (0:04:39)。其中16项为本轮新增的阅读布局与真实Excel操作测试；其余既有测试继续执行。5条提示均来自原日期解析异常测试，相关后端文件没有修改。
- 原计算结果对照：4个构造场景，在人工选择前后逐项比较事项索引、候选选项、候选组成、原始明细、月度统计与数值汇总，全部一致；20个原有核心文件指纹一致。
- 真实应用入口：输入15条银行记录、12条序时账记录，输出完整27条原始记录，人工全查4项、人工抽样4项。原输入文件哈希不变，生成用时4.41秒；Excel完整重算并保存回读，错误公式0个。
- 视觉验收：最终样例12个月与1行全年合计同表；默认7张主表各导出1页并查看月度、整组、人工表；整组展开后完整原始组成保留。附3页月度、整组和人工全查预览。
- 原启动入口检查返回READY。筛选视图经现有另存入口生成；测试核验整组原记录、原公式和填写结果完整保留。
- 报告层规模测试：1000组、2000条原始记录，构表0.843秒，写入排版48.03秒，Excel完整重算0.135秒；错误公式0个，原始记录2000条，双向已对平金额覆盖率均为100%。这只验证报告层，不作为匹配算法准确率证明。

## 交付文件与使用范围

交付样例：C:\Users\27651\Documents\Codex\2026-09-06\c-users-27651-documents-code\outputs\银行流水核对报告-阅读优化样例.xlsx。

筛选样例：C:\Users\27651\Documents\Codex\2026-09-06\c-users-27651-documents-code\outputs\银行流水核对报告-阅读优化筛选视图.xlsx。

预览文件：C:\Users\27651\Documents\Codex\2026-09-06\c-users-27651-documents-code\outputs\银行流水核对报告-月度整组人工预览.pdf。

本次样例全部是构造数据，用于验证阅读和操作，不是对真实审计项目作结论。使用源码启动入口重新生成报告才会得到新版布局；此前保存的报告不会自动转换。独立EXE本轮没有重建或验证。

人工选择沿用原效果：确认对应不等于消除金额差额，不改变原始发生月份或月末余额。初始分组和原始单边位置保持稳定，查看“当前核对结论”可知道记录现在归入哪项。原有专题可取消隐藏查看，无新增资料要求。

本轮交付按原授权在现有工作分支提交推送；GitHub提交记录是版本依据。没有合并主分支，也没有重写远端历史。

## 核验索引

最终完整测试日志：C:\Users\27651\Documents\Codex\2026-09-06\c-users-27651-documents-code\work\展示定稿全量03-测试结果.txt。

前后计算对照：C:\Users\27651\Documents\Codex\2026-09-06\c-users-27651-documents-code\work\展示计算边界_基线_184507\证据.json、C:\Users\27651\Documents\Codex\2026-09-06\c-users-27651-documents-code\work\展示计算边界_验收_191149\证据.json。

真实入口与视觉证据：C:\Users\27651\Documents\Codex\2026-09-06\c-users-27651-documents-code\work\报告展示交付验收_191229\验收结果.json；规模测试证据：C:\Users\27651\Documents\Codex\2026-09-06\c-users-27651-documents-code\work\千组展示性能验收_20260906_191648_750096\计时证据.json。

交付文件SHA256：

- 银行流水核对报告-阅读优化样例.xlsx：9a2b126099dc914074d22de2b51f38a0e44ff8dca6011ca7c3f4a9633c3f5b42
- 银行流水核对报告-阅读优化筛选视图.xlsx：a8230b2e0f3edb930c176c4d481c4b9a685e1a8a1b0053747c69be9585dea6ea
- 银行流水核对报告-月度整组人工预览.pdf：ff84ba7e52a5e3a11f49f3a2771d5bc6dba3e514b4540798582a7c73e98bc3bf

# 余额断档局部降级验收审查（2026-09-06）

需求：`docs/requirements/2026-09-06-余额断档局部降级-requirement.md`；设计：`docs/superpowers/specs/2026-09-06-余额断档局部降级-design.md`；计划：`docs/superpowers/plans/2026-09-06-余额断档局部降级.md`。

## 一、审查范围

`git diff 85a3874`：balance.py（窗口标注＋局部化判定）、data_structures.py（新字段）、matcher.py（按窗口盖戳）、reporter.py（系统结论）、input_precheck.py（检查项说明）、tests/test_余额断档局部降级.py（6 项新测试）、readme.md、CONTEXT.md、2026-09-05 设计文档第 46 条。

## 二、逐项对照需求

1. 断档可定位时仅窗口内降级 ✓（matcher 按候选任一侧记录日期落窗判定；测试 1、2、5）
2. 窗口口径＝断档日＋前一有记录交易日 ✓（check_balance_continuity 用前一锚点日；测试 1）
3. 任一侧落入即降级 ✓（测试 5 跨窗口样本）
4. 系统结论披露断档日期与区间 ✓（reporter/input_precheck；测试 6）
5. 待查记录与连续性异常表行为不变 ✓（未触碰；478 项全量回归通过）

边界：期间无法计算、口径未验证、解析异常维持整体降级 ✓（test_独立复核修复.py::test_结构性问题仍然限制范围并降级 等 22 项定向回归通过）；断档无法定位或覆盖过半回退整体降级 ✓（测试 3）。

## 三、正确性审查发现

- `previous_anchor_day` 与 `previous_closing` 同步维护：previous_closing 非空 ⟹ 锚点日非空，日界断档窗口必然完整，无 None 漏出路径。
- "仅有 1 个有效余额点""最后交易行未提供余额"两类原因不含"连续性/期初加净发生额"字样，局部化时被正确保留为整体降级理由（保守方向）。
- 整体降级与局部断档并存时（其他结构性问题同时为真）：matcher 第一分支仍全量盖戳，reporter 结论落到整体文案——语义正确。
- 候选日期取自 `self.bank/self.journal` 与 `_business_rows` 同一索引域，NaT 已过滤。
- 变更不变异审查：去掉盖戳→测试 4 失败；去掉覆盖过半判定→测试 3 失败；去掉窗口键→测试 1、2 失败；文案回退→测试 6 失败。每个现实变异均有测试兜底。

## 四、过度工程审查（ponytail-review）

无 reinvented stdlib、无推测性抽象、无 dead flexibility。相邻断档窗口不合并、按候选逐次 .loc 取日期均为有意保留的最简实现（候选规模千级，O(k) 可查）。结论：Lean already。

## 五、验证证据

- 新增 6 项测试均经 RED（缺窗口键 KeyError、降级 0 笔、结论无断档字样）→ GREEN。
- 全量测试 478 项通过（109 秒，含 472 项既有回归＋6 项新增，无既有预期改动）。
- 真实数据验证（东方农博民生银行，405 秒重跑）：系统结论改为"余额在2025-07-01、2026-07-01断档，仅断档窗口内关系降为疑点，其余关系按证据正常确认"；核对范围恢复"范围可用"；范围未知事项由 2,354 笔降至 19 笔（恰为两断档窗口内关系：2025 窗口 9 笔、2026 窗口 10 笔）；疑点事项恢复风险分级（高 108／中 672／低 1,555）；余额连续性异常仍为 2 处真实断档，待查 68/220 笔不变。
- 新发现（超出本次修复范围）：疑点事项仍为 2,291 笔，主因是 2026-09-05 批准的既有保守规则——"金额和日期相同但业务依据不足"（2,211 笔，该数据两侧无共同业务编号、摘要口径不同）与候选歧义（2,204 笔，高重复金额）。是否放宽须另行决策，不在本次改动内。

## 六、残余风险

1. 断档窗口口径由用户拍板（断档日＋前一交易日）；更保守口径（整月）未实现，需要时再议。
2. 日内勾稽失败的窗口按当日全天计，缺行若跨日则窗口偏窄——构造样本未覆盖，真实数据中未出现该形态。
